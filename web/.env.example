# Base URL of the LabReserve backend (outputs/ server).
# Local dev: run the backend with `node server.js` in outputs/ (default port 8775).
# Production (Netlify): set this to the deployed backend URL, e.g. https://labreserve-api.onrender.com
NUXT_LEGACY_API_BASE=http://localhost:8775
